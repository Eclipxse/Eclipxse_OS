# ISO layer

This directory will hold the Debian 13 `live-build` configuration after the
Phase 0 desktop concept is approved. Keeping it empty until then prevents theme
iteration from being confused with operating-system integration work.

